import React, { useState } from 'react';

function LoginButton() {

    return (
        <div>
            
            <button className='google_button'> Авторизоваться через Google! </button>
        </div >
    );
}

export default LoginButton;